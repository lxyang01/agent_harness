upstream billguard {
    ip_hash;
    server web-1:8001;
    server web-2:8002;
}
server {
    listen 80;
    location / {
        proxy_pass http://billguard;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
