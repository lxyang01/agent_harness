# 用法:复制到仓库根目录为 .env(compose 启动时自动读取,已 gitignore,不会提交)。
# 只需放"宿主机才有的值":模型 Key 与可选的模型选择;集群内部地址 compose 已内置。

# 必填(真实对话):模型 Key(写 OPENROUTER_API_KEY=... 同样有效)
API_KEY=sk-or-v1-你的key

# 可选:换模型/换端点(默认 openai/gpt-4.1-mini @ https://openrouter.ai/api/v1)
#BILLGUARD_LLM_MODEL=openai/gpt-4.1-mini
#BILLGUARD_LLM_BASE_URL=https://openrouter.ai/api/v1
